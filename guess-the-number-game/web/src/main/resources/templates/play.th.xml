<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head::headFragment}" />
    <attr sel="#main-sessage"
          th:text="${__${T(com.ankit.util.AttributeNames).MAIN_MESSAGE}__}" />
    <attr sel="#result-message"
          th:text="${__${T(com.ankit.util.AttributeNames).RESULT_MESSAGE}__}" />
    <attr sel="#game-form"
          th:action="${__${T(com.ankit.util.GameMappings).PLAY}__}" />
    <attr sel="#footerId"
          th:replace="~{fragments/footer::footFragment}" />
</thlogic>